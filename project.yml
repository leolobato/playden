name: BigScreen
options:
  deploymentTarget:
    macOS: '15.0'
settings:
  base:
    SWIFT_VERSION: '6.0'
    MACOSX_DEPLOYMENT_TARGET: '15.0'
configFiles:
  Debug: Config/Build.xcconfig
  Release: Config/Build.xcconfig
packages:
  BigScreenKit:
    path: Packages/BigScreenKit
targets:
  BigScreen:
    type: application
    platform: macOS
    sources:
      - path: App
    postBuildScripts:
      - path: scripts/embed-display-helper.sh
        name: Build Windows display helper
        basedOnDependencyAnalysis: false
        outputFiles:
          - '$(TARGET_BUILD_DIR)/$(UNLOCALIZED_RESOURCES_FOLDER_PATH)/BigScreenDisplay.exe'
      - path: scripts/embed-runtime.sh
        name: Embed Steam runtime libraries
        basedOnDependencyAnalysis: false
        outputFiles:
          - '$(TARGET_BUILD_DIR)/$(FRAMEWORKS_FOLDER_PATH)/liblzma.5.dylib'
          - '$(TARGET_BUILD_DIR)/$(FRAMEWORKS_FOLDER_PATH)/libzstd.1.dylib'
          - '$(TARGET_BUILD_DIR)/$(UNLOCALIZED_RESOURCES_FOLDER_PATH)/Licenses/liblzma.txt'
          - '$(TARGET_BUILD_DIR)/$(UNLOCALIZED_RESOURCES_FOLDER_PATH)/Licenses/zstd.txt'
    dependencies:
      - package: BigScreenKit
        product: Domain
      - package: BigScreenKit
        product: Focus
      - package: BigScreenKit
        product: Input
      - package: BigScreenKit
        product: Catalog
      - package: BigScreenKit
        product: Sources
      - package: BigScreenKit
        product: Runner
      - package: BigScreenKit
        product: Installs
      - package: BigScreenKit
        product: Sessions
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: $(BIGSCREEN_BUNDLE_IDENTIFIER)
        PRODUCT_NAME: Big Screen
        PRODUCT_MODULE_NAME: BigScreen
        MARKETING_VERSION: '0.1'
        CURRENT_PROJECT_VERSION: '1'
        HEADER_SEARCH_PATHS: ['$(inherited)', /opt/homebrew/include]
        LIBRARY_SEARCH_PATHS: ['$(inherited)', /opt/homebrew/lib]
        LD_RUNPATH_SEARCH_PATHS: ['$(inherited)', '@executable_path/../Frameworks', '@loader_path/../Frameworks']
        GENERATE_INFOPLIST_FILE: YES
        INFOPLIST_KEY_CFBundleDisplayName: Big Screen
        INFOPLIST_KEY_LSApplicationCategoryType: public.app-category.games
        INFOPLIST_KEY_NSRemovableVolumesUsageDescription: 'Big Screen installs and plays games on the drive you choose.'
        CODE_SIGN_STYLE: Automatic
        CODE_SIGN_IDENTITY: '-'
        ENABLE_APP_SANDBOX: NO
  BigScreenTests:
    type: bundle.unit-test
    platform: macOS
    sources: [Tests/AppTests]
    dependencies:
      - target: BigScreen
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: $(BIGSCREEN_BUNDLE_IDENTIFIER).tests
        GENERATE_INFOPLIST_FILE: YES
        CODE_SIGN_IDENTITY: '-'
        TEST_HOST: '$(BUILT_PRODUCTS_DIR)/Big Screen.app/Contents/MacOS/Big Screen'
schemes:
  BigScreen:
    build:
      targets:
        BigScreen: all
    test:
      targets: [BigScreenTests]
